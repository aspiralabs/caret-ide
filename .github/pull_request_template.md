## What & why

<!-- What does this change do, and why? Link any related issue (e.g. Closes #12). -->

## How I verified

<!-- e.g. npm run typecheck, npm run build, manual steps in npm run dev, smoke test -->

- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] Verified the behavior manually

## Notes for reviewers

<!-- Anything worth calling out: trade-offs, follow-ups, screenshots. -->
